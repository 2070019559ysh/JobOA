@model List<JobOA.Model.Employee>
           @foreach (var employee in Model)
           {
            <img src="@employee.HeadPicture" width="20" height="20"/>
               @employee.RealName
           }